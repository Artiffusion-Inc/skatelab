// AUTO-GENERATED — do not edit. Source: DESIGN.md
package ru.skatelab.capture.presentation.theme

import androidx.compose.ui.graphics.Color

object SkateLabColors {
    // ── Brand & Accent ──────────────────────────────────────────────
    val primary = Color(0xFF155F73)
    val primaryDeep = Color(0xFF0E3340)
    val primaryForeground = Color(0xFFFFFFFF)

    // ── Text ────────────────────────────────────────────────────────
    val ink = Color(0xFF2A2D2E)
    val inkMute = Color(0xFF6B7275)
    val inkFaint = Color(0xFF9BA0A3)

    // ── Surface ─────────────────────────────────────────────────────
    val canvas = Color(0xFFFFFFFF)
    val canvasSoft = Color(0xFFF5F7F8)
    val surfaceIceSoft = Color(0xFFC8E6F0)
    val surfaceTealDeep = Color(0xFF0E3340)
    val surfaceTealMid = Color(0xFF155F73)

    // ── Border ──────────────────────────────────────────────────────
    val hairline = Color(0xFFD5DDE0)
    val hairlineDark = Color(0xFF2A4A52)

    // ── On-Dark Text ────────────────────────────────────────────────
    val onDarkMute = Color(0xFFC5D5DB)
    val onDarkDim = Color(0xFF8AABB8)
    val onDarkFaint = Color(0xFF5A7A85)

    // ── Semantic ────────────────────────────────────────────────────
    val destructive = Color(0xFFC0392B)
    val link = Color(0xFF155F73)
    val ring = Color(0xFF155F73)
    val scoreGood = Color(0xFF27AE60)
    val scoreMid = Color(0xFFF39C12)
    val scoreBad = Color(0xFFE74C3C)
    val accentGold = Color(0xFFF39C12)

    // ── Semantic Aliases (shadcn/ui compatibility) ──────────────────
    val background: Color get() = canvas
    val foreground: Color get() = ink
    val card: Color get() = canvas
    val cardForeground: Color get() = ink
    val popover: Color get() = canvas
    val popoverForeground: Color get() = ink
    val secondary: Color get() = canvasSoft
    val secondaryForeground: Color get() = ink
    val muted: Color get() = canvasSoft
    val mutedForeground: Color get() = inkMute
    val accent: Color get() = canvasSoft
    val accentForeground: Color get() = ink
    val destructiveForeground: Color get() = primaryForeground
    val border: Color get() = hairline
    val input: Color get() = hairline
    val chart1: Color get() = primary
    val chart2: Color get() = scoreGood
    val chart3: Color get() = scoreMid
    val chart4: Color get() = scoreBad
    val chart5: Color get() = accentGold
    val sidebarBackground: Color get() = canvas
    val sidebarForeground: Color get() = ink
    val sidebarPrimary: Color get() = primary
    val sidebarPrimaryForeground: Color get() = primaryForeground
    val sidebarAccent: Color get() = canvasSoft
    val sidebarAccentForeground: Color get() = ink
    val sidebarBorder: Color get() = hairline
    val sidebarRing: Color get() = ring
}
