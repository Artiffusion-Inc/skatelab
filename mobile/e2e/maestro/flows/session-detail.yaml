appId: ru.skatelab.capture
tags:
  - sessions
---
# Session detail requires at least one session to exist
# This flow is skipped if no sessions are present
- launchApp
- assertVisible: "Camera"
- tapOn: "Sessions"
# If sessions exist, tap the first one
# If no sessions, this flow will fail gracefully
- assertVisible: "Profile"