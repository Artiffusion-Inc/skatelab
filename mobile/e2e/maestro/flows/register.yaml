appId: ru.skatelab.capture
tags:
  - auth
---
- launchApp
# Grant camera permission if prompted
- runFlow:
    when:
      visible: "While using the app"
    commands:
      - tapOn: "While using the app"
# Navigate to register screen from login
- assertVisible: "Log in to your account"
- tapOn: "Don't have an account? Register"
- assertVisible: "Create Account"
- tapOn: "Display name"
- inputText: Test User
- back
- tapOn: "Email"
- inputText: test@skatelab.ru
- back
- tapOn: "Password"
- inputText: Test123456
- back
- tapOn: "Register"
# After register attempt without backend: retry button should appear
- extendedWaitUntil:
    visible: "Retry"
    timeout: 10000