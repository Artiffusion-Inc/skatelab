appId: ru.skatelab.capture
tags:
  - smokeTest
  - recording
---
# Recording flow - requires real camera, will not work on emulator
# On emulator, Camera screen shows upload-only mode
- launchApp
- extendedWaitUntil:
    visible: "Camera"
    timeout: 15000
- assertVisible: "Upload video"