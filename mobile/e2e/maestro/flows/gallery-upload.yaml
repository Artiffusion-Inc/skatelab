appId: ru.skatelab.capture
tags:
  - upload
---
# Gallery upload flow - requires camera permission and gallery video
# This flow tests the gallery upload button visibility on the Camera screen
- launchApp
- extendedWaitUntil:
    visible: "Camera"
    timeout: 15000
- assertVisible: "Upload video"