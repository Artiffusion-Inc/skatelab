# Dokploy Postgres 17 service env. Replace __FROM_DOKPLOY_SECRETS__ with real values
# or set via Dokploy Secrets store. Mirrors /opt/infra/compose.yaml postgres service.
POSTGRES_USER=skatelab
POSTGRES_PASSWORD=__FROM_DOKPLOY_SECRETS__
POSTGRES_DB=skatelab