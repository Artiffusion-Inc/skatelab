# Agent Tooling

- For unfamiliar code located by behavior, prefer semantic/structural search before broad file reads. Exact symbol, string, regex, or known path uses `rg`/direct read.
- `.claude/worktrees/` contains stale historical worktrees. Do not treat copied instruction files there as current project policy.
- Do not edit or merge stale worktree contents unless user explicitly identifies one.
