# T053 brief — Fix #416 (i18n residual)

Two sites, same bug class (hardcoded Russian bypassing locale):

## Site A: mobile SessionListScreen raw messageKey
File: `mobile/androidApp/src/main/java/ru/skatelab/capture/ui/session/SessionListScreen.kt`
- :123 `val message = (uiState as SessionsUiState.Error).error.messageKey`
- :131 `Text(message, ...)` renders raw key (e.g. `error_network`)

Fix: reuse existing `AppError.localizedMessage()` composable (in `utils/AppErrorExt.kt`).
- Import `ru.skatelab.capture.utils.localizedMessage`
- Replace `val message = ...error.messageKey` + `Text(message, ...)` with `Text((uiState as SessionsUiState.Error).error.localizedMessage(), ...)`
- Match the exact pattern used in DashboardScreen:90 / SessionDetailScreen:122 / MetricTrendScreen:140.

Verify: `grep 'messageKey' SessionListScreen.kt` → empty. Docker `:androidApp:testDebugUnitTest ktlintCheck` BUILD SUCCESSFUL.

## Site B: frontend offer/terms pages fully hardcoded Russian
Files:
- `frontend/src/app/(landing)/offer/page.tsx`
- `frontend/src/app/(landing)/terms/page.tsx`

Both are server components (no "use client"), use `LegalLayout` (which uses `getTranslations("common")`). Pattern for server components: `getTranslations` from `next-intl/server`.

Hardcoded Russian literals to localize (in BOTH pages, identical structure):
- breadcrumb: `Главная` (also `Правовая информацию` label, `<span>Оферта</span>` / `<span>Пользовательское соглашение</span>`)
- `<h1>` `Оферта` / `Пользовательское соглашение`
- `<p>` `Документ готовится.`
- `<Link>` `На главную →`

Reuse `common.home` (already exists) for `На главную`. Add namespaces `offer` and `terms` to `frontend/messages/en.json` + `ru.json`.

Keys to add (en/ru):
- `offer.title` — "Offer" / "Оферта"
- `offer.comingSoon` — "Document in preparation." / "Документ готовится."
- `offer.legalInfo` — "Legal information" / "Правовая информация"  (breadcrumb label, shared with terms)
- `offer.home` — reuse `common.home` instead (do NOT duplicate)
- `terms.title` — "Terms of Service" / "Пользовательское соглашение"
- `terms.comingSoon` — reuse `offer.comingSoon`? NO — keep separate `terms.comingSoon` same text, OR put `comingSoon` in a shared `legal` namespace. SIMPLEST: add `offer.comingSoon` + `terms.comingSoon` separately (both "Документ готовится." / "Document in preparation."). Minimal, no new namespace.
- breadcrumb "Главная" → `common.home` (reuse, already exists)
- "На главную →" link → use `common.home` + literal "→"? Keep "→" as literal (arrow is locale-agnostic). Render `{t("home")} →` or `{t("home")} →`. Check legal-layout renders `t("home")` plain. Match: `На главную →` → `{common t("home")} →`. The arrow stays literal.

NOTE on breadcrumb "Правовая информацию" — actually text is `Правовая информация`. Add to BOTH offer+terms under shared key. Put it in `common.legalInfo` to avoid dup across offer/terms. Add `common.legalInfo`: "Legal information" / "Правовая информация".

### Page becomes (offer example):
```tsx
import type { Metadata } from "next"
import Link from "next/link"
import { getTranslations } from "next-intl/server"
import LegalLayout from "../legal-layout"

export const metadata: Metadata = {
  title: "Оферта — SkateLab",
}

export default async function OfferPage() {
  const t = await getTranslations("offer")
  const tCommon = await getTranslations("common")
  return (
    <LegalLayout>
      <nav className="mb-6 sh-caption text-ink-mute">
        <a href="/" className="hover:text-ink">
          {tCommon("home")}
        </a>
        {" > "}
        <span>{tCommon("legalInfo")}</span>
        {" > "}
        <span>{t("title")}</span>
      </nav>
      <h1 className="sh-display-lg text-ink mb-8">{t("title")}</h1>
      <p className="sh-body-lg text-ink-mute">{t("comingSoon")}</p>
      <p className="mt-4">
        <Link href="/" className="sh-button-cap text-link hover:underline">
          {tCommon("home")} →
        </Link>
      </p>
    </LegalLayout>
  )
}
```
terms analogous with `getTranslations("terms")`.

`metadata.title` stays hardcoded Russian (Next.js static export — requires generateMetadata for locale, out of scope; visible CONTENT is the bug). Leave a one-line comment is NOT required.

## Verification
- `grep 'error.messageKey' mobile/.../SessionListScreen.kt` → empty
- `grep -c 'getTranslations' frontend/src/app/(landing)/offer/page.tsx frontend/src/app/(landing)/terms/page.tsx` → >=1 each
- `grep -E 'Оферта|Пользовательское соглашение|Документ готовится|На главную|Правовая информация' frontend/src/app/(landing)/offer/page.tsx frontend/src/app/(landing)/terms/page.tsx` → 0 matches in .tsx (only `metadata.title` Оферта/Пользовательское соглашение remain — acceptable, it's static SEO meta, not visible body content; OR localize via generateMetadata — NOT required)
- `cd frontend && bunx tsc --noEmit` → clean
- Docker `:androidApp:testDebugUnitTest ktlintCheck` → BUILD SUCCESSFUL

## Constraints
- Edit ONLY: SessionListScreen.kt, offer/page.tsx, terms/page.tsx, frontend/messages/en.json, frontend/messages/ru.json
- Match existing patterns (localizedMessage composable, getTranslations server pattern from legal-layout)
- Commit after each site with `fix(mobile):` / `fix(frontend):` convention
- Do NOT touch metadata.title (out of scope), do NOT touch privacy/page.tsx (legal doc, RU jurisdiction — issue explicitly excludes it)
